# Player Models

Primary slot:

- `vincent-hero.glb`

Keep the replacement model under `1 MB` where practical. Preserve named parts such as `LeftArm`, `RightArm`, `LeftLeg`, `RightLeg`, and `Cape`, or provide internal clips named in `lib/model-registry.ts`.
