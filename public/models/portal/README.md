# Portal Models

Primary slot:

- `contact-portal.glb`

The current portal uses a procedural ring-and-light-column fallback. A future GLB can provide `Idle`, `Pulse`, and `Activate` clips. The Guide Spirit has its own independent slot under `../npc/guide-spirit.glb`.
