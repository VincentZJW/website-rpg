# Boss Models

Primary slot:

- `career-fog-dragon.glb`

Keep the Boss model under `2 MB` where practical. Recommended animation clips are `Idle`, `Pulse`, and `Roar`; named parts such as `Head`, `LeftWing`, `RightWing`, `TailOne`, and `TailTwo` retain procedural motion when clips are absent.
